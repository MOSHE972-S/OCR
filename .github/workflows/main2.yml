name: OCR-Ultra-Build

on:
  workflow_dispatch:

permissions:
  contents: write 

jobs:
  build:
    runs-on: windows-latest
    env:
      PYTHONUTF8: 1

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Setup Python 3.11
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip'

    - name: Create Folders
      shell: powershell
      run: |
        New-Item -ItemType Directory -Force -Path data, weights, checkpoints, gui

    - name: Install Dependencies (Fast CPU Version)
      run: |
        python -m pip install --upgrade pip
        pip install "numpy<2.0.0"
        # התקנת גרסת CPU חוסכת זמן בנייה ומשקל ענקי
        pip install torch==2.3.1 torchvision==0.18.1 --index-url https://download.pytorch.org/whl/cpu
        pip install onnxruntime==1.18.1 onnx opencv-python-headless albumentations pandas PyQt6 Pillow jiwer pyinstaller

    - name: Build EXE (Fixed Path & ONNX Crash)
      shell: powershell
      run: |
        # פותר את בעיית ה-ModuleNotFoundError
        $env:PYTHONPATH = "."
        
        # הרצה עם החרגת הגורמים לקריסה (onnx.reference)
        pyinstaller --noconfirm --onedir --windowed `
        --name "MyCustomOCR" `
        --add-data "data;data" `
        --add-data "weights;weights" `
        --add-data "checkpoints;checkpoints" `
        --collect-binaries onnxruntime `
        --collect-binaries onnx `
        --exclude-module onnx.reference `
        --exclude-module matplotlib `
        --exclude-module notebook `
        --exclude-module scipy.spatial.tests `
        --exclude-module tkinter `
        --exclude-module unittest `
        --hidden-import gui `
        --paths "." `
        --noupx `
        gui/app.py

    - name: Compress & Release
      shell: powershell
      run: |
        if (Test-Path dist/MyCustomOCR) {
            # דחיסה מקסימלית ב-7z
            & "C:\Program Files\7-Zip\7z.exe" a -tzip -mx=9 MyOCR_Final.zip .\dist\MyCustomOCR\*
        } else {
            Write-Error "Build failed!"
            exit 1
        }

    - name: Upload to Github Releases
      uses: softprops/action-gh-release@v2
      with:
        files: MyOCR_Final.zip
        tag_name: v4.0.${{ github.run_number }}
        name: OCR Release v4.0.${{ github.run_number }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
